service: instagrow

frameworkVersion: ">=1.0.0 <2.0.0"

provider:
  name: aws
  runtime: nodejs6.10
  profile: instagrow
  timeout: 290
  versionFunctions: false
  region: ca-central-1
  vpc:
    securityGroupIds:
      - sg-58292b30
    subnetIds:
      - subnet-252a395e
      - subnet-722a3909
  environment:
    ACCOUNT: ${file(./serverless.env.yml):${opt:stage}.ACCOUNT}
    PASSWORD: ${file(./serverless.env.yml):${opt:stage}.PASSWORD}
    FOLLOWING_INTERACTION_DELTA_IN_DAYS: ${file(./serverless.env.yml):${opt:stage}.FOLLOWING_INTERACTION_DELTA_IN_DAYS}
    FOLLOWER_INTERACTION_DELTA_IN_DAYS: ${file(./serverless.env.yml):${opt:stage}.FOLLOWER_INTERACTION_DELTA_IN_DAYS}
  iamRoleStatements: # permissions for all of your functions can be set here
    - Effect: Allow
      Action: # Gives permission to DynamoDB tables in a specific region
        - dynamodb:DescribeTable
        - dynamodb:Query
        - dynamodb:Scan
        - dynamodb:GetItem
        - dynamodb:PutItem
        - dynamodb:UpdateItem
        - dynamodb:DeleteItem
      Resource: "arn:aws:dynamodb:ca-central-1:*:*"

functions:
  setUpNewApplication:
    handler: handler.setUpNewApplication
  setUpNewUser:
    handler: handler.setUpNewUserConfig
  setUpScalingPolicy:
    handler: handler.setUpScalingPolicy
  functionHandler:
    handler: handler.functionHandler
  getFollowing:
    handler: handler.getFollowing
    events:
      - schedule:
          enabled: true
          rate: cron(10,20,30,40 11 ? * * *)
  getFollowers:
    handler: handler.getFollowers
    events:
      - schedule:
          enabled: true
          rate: cron(15,25,35,45 11 ? * * *)
  updateInteractionActivity:
    handler: handler.updateInteractionActivity
    events:
      - schedule:
          enabled: false
          rate: cron(40 0-4 ? * * *)
      - schedule:
          enabled: false
          rate: cron(40 11-13 ? * 1-5 *)
      - schedule:
          enabled: false
          rate: cron(40 13-16 ? * 6-7 *)
  queuePendingLikeMedia:
    handler: handler.queuePendingLikeMedia
    events:
      - schedule:
          enabled: false
          rate: cron(50 0-4 ? * * *)
      - schedule:
          enabled: false
          rate: cron(50 11-13 ? * 1-5 *)
      - schedule:
          enabled: false
          rate: cron(50 13-16 ? * 6-7 *)
  updateLikedMedia:
    handler: handler.updateLikedMedia
    events:
      - schedule:
          enabled: false
          rate: cron(*/10 1-5 ? * * *)
      - schedule:
          enabled: false
          rate: cron(*/10 12-14 ? * 1-5 *)
      - schedule:
          enabled: false
          rate: cron(*/10 14-17 ? * 6-7 *)

plugins:
  - serverless-dynamodb-local
  - serverless-offline
  - serverless-webpack
  - serverless-plugin-aws-alerts

custom:
  webpackIncludeModules: true
  dynamodb:
    start:
      port: 8000
      migrate: true
      noStart: true
  alerts:
    stages:
      - dev
      - prod
    topics:
      alarm:
        topic: ${self:service}-${opt:stage}-alerts-alarm
        notifications:
          - protocol: email
            endpoint: instagrow@aaronromeo.com # Change this to your email address
    alarms:
      - functionErrors
      - functionThrottles